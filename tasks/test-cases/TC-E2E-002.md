---
tc_id: TC-E2E-002
title: 导航无 JS 错误
status: passed
layer: e2e
req_ref: [REQ-013]
priority: P0
spec_file: frontend/tests/e2e/smoke.spec.ts
spec_name: "App shell smoke › navigation renders without JS errors"
---

## 测试目标

验证应用加载至 `networkidle` 状态全程无未捕获的 JavaScript 运行时错误。

## 前置条件

- `GET **/diagnosis/run` → mock SSE (minimal)
- `GET **/health` → mock `{"status":"ok"}`
- 监听 `page.on("pageerror")` 收集错误

## 操作步骤

1. 访问 `/`
2. 等待 `networkidle`
3. 断言 `pageerror` 收集数组为空

## 预期结果 / 不应发生

- `errors` 数组长度为 0
- 不应发生：任何未捕获异常、React hydration 错误、模块加载失败
