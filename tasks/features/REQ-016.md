---
req_id: REQ-016
title: JWT 鉴权 API 与中间件
status: draft
priority: P2
phase: phase-4
owner: unassigned
depends_on: []
test_case_ref: []
scope: backend
acceptance: 未认证请求返回 401，/auth/login 与 /auth/refresh 接口就绪
---

# Goal

为 FastAPI 应用添加基于 JWT 的身份认证，保护诊断 API，并提供登录和 token 刷新接口。

# In Scope

- `POST /auth/login`：用户名/密码验证，返回 `access_token` 和 `refresh_token`
- `POST /auth/refresh`：刷新 access token
- JWT 中间件：未携带有效 token 的请求返回 401
- FastAPI `Depends` 注入认证逻辑
- 环境变量：`JWT_SECRET_KEY`、`JWT_ALGORITHM`、`ACCESS_TOKEN_EXPIRE_MINUTES`

# Out of Scope

- 用户管理 UI（见 REQ-022）
- 多租户隔离（见 REQ-021）
- 审计日志（见 REQ-018）

# Acceptance Criteria

- 未携带 token 请求 `/diagnosis/run` 返回 401
- `POST /auth/login` 返回有效 JWT
- `POST /auth/refresh` 使用 refresh_token 返回新 access_token

# Test Case Design Notes

> 需覆盖：有效 token 通过认证、过期 token 返回 401、无效签名返回 401、refresh_token 刷新成功。

# Agent Notes

此项为 Phase-004 安全加固的基础依赖，REQ-017/018/021/022/023 均依赖于此。
