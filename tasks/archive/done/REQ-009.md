---
req_id: REQ-009
title: AutoDiagnosisService 后台 Worker
status: done
priority: P1
phase: phase-2
owner: unassigned
depends_on: [REQ-008, REQ-001]
test_case_ref:
  - tests/test_auto_diagnosis/test_auto_diagnosis_service.py::TestStartStopStateMachine
  - tests/test_auto_diagnosis/test_auto_diagnosis_service.py::TestQueuedAtStability
  - tests/test_auto_diagnosis/test_auto_diagnosis_service.py::TestCurrentLifecycle
  - tests/test_auto_diagnosis/test_auto_diagnosis_runner.py::TestRunAutoDiagnosis
scope: backend
acceptance: Worker 消费故障队列调用 graph.ainvoke()，同一机组在冷却期内不重复触发
---

# Goal

实现后台诊断 Worker，消费 FaultAggregator 产生的故障队列，自动调用 LangGraph 诊断 Pipeline 并将结果写入 DiagnosisStore。

# In Scope

- `auto_diagnosis.py` 中的 `AutoDiagnosisService`
- 故障队列消费循环
- 自动诊断专用 LangGraph：在手动诊断 Pipeline 前插入 `sensor_reader` 节点（`build_auto_graph()`）
- 调用 `graph.ainvoke()` 执行诊断
- 冷却期检查（`DIAGNOSIS_COOLDOWN` 环境变量）
- 结果写入 `diagnosis_store`（环形缓冲区）

# Out of Scope

- 故障聚合（见 REQ-008）
- 结果 API 暴露（见 REQ-010）

# Acceptance Criteria

- Worker 消费故障队列并调用 `graph.ainvoke()`
- 同一 `unit_id` 在 `DIAGNOSIS_COOLDOWN` 秒内不重复触发诊断
- 诊断结果写入 `diagnosis_store` 环形缓冲区

# Test Case Design Notes

> 需覆盖：正常诊断完成写入、冷却期过滤、graph 异常时的错误处理。

| Acceptance Criteria | 覆盖状态 | 测试函数 |
|---|---|---|
| Worker 消费故障队列并调用 graph.ainvoke() | ✅ | `TestRunAutoDiagnosis` |
| 同一 unit_id 在 DIAGNOSIS_COOLDOWN 秒内不重复触发 | ✅ | `test_fault_aggregator.py::TestFaultTrigger::test_fault_second_poll_in_cooldown_returns_none`（冷却抑制在 FaultAggregator 层实施）；`test_fault_aggregator.py::TestPollingLoop::test_on_fault_called_once_within_cooldown`（轮询循环级验证） |
| 诊断结果写入 diagnosis_store 环形缓冲区 | ✅ | `TestCurrentLifecycle` |

# Agent Notes

已在 PHASE-002 中实现。位于 `backend/app/agents/auto_diagnosis.py`。冷却期由 `DIAGNOSIS_COOLDOWN` 环境变量控制（默认 300 秒）。

code_refs:
  - backend/app/agents/auto_diagnosis.py   # AutoDiagnosisService
  - backend/app/agents/graph.py            # build_auto_graph(), node:sensor_reader
  - backend/app/agents/sensor_reader.py    # sensor_reader_node
