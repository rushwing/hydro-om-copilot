---
req_id: REQ-006
title: 会话历史持久化
status: done
priority: P1
phase: phase-1
owner: unassigned
depends_on: [REQ-005]
test_case_ref: []
scope: frontend
acceptance: 页面刷新后 localStorage 中的会话历史可还原，历史 DiagnosisResult 列表无数据丢失
---

# Goal

将诊断会话历史持久化到 localStorage，确保页面刷新后用户仍可访问历史诊断记录。

# In Scope

- `useSessionHistory` hook 的 localStorage 读写逻辑
- `HistoryPage`：会话历史列表渲染页面（含手动诊断与自动诊断历史）
- 历史记录的加载与还原

# Out of Scope

- 服务端历史存储（当前只用 localStorage）
- 历史记录导出/分享
- 跨设备同步

# Acceptance Criteria

- 页面刷新后 `localStorage` 中的会话历史完整还原
- 历史 `DiagnosisResult` 列表字段无数据丢失
- 最多保留最近 50 条记录（`MAX_HISTORY = 50`，超出时淘汰最旧条目，防止 localStorage 溢出）

# Test Case Design Notes

> 需覆盖：刷新后历史还原、第 51 条记录写入时最旧条目被淘汰（上限 50）、空历史的初始状态。

# Agent Notes

已在 PHASE-001 中实现。逻辑在 `frontend/src/hooks/useSessionHistory.ts`（架构约束文件，不可修改）。

code_refs:
  - frontend/src/hooks/useSessionHistory.ts   # useSessionHistory
  - frontend/src/pages/HistoryPage.tsx         # HistoryPage
