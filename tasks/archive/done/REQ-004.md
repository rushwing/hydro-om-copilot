---
req_id: REQ-004
title: 知识库入库流水线
status: done
priority: P1
phase: phase-1
owner: unassigned
depends_on: []
pytest_ref:
  - tests/test_rag/test_ingest.py::test_ingest_creates_chroma_collection
xfail_ref:
  - tests/test_rag/test_ingest.py::test_ingest_is_idempotent  # xfail(strict=True): AC not yet met
test_case_ref: []
scope: backend
acceptance: scripts/ingest_kb.py 执行后 ChromaDB 与 BM25 索引均可被 HybridRetriever 查到新入库文档
---

# Goal

提供一键式知识库入库脚本，将 Markdown 格式的运维知识文档解析、分块、嵌入，并写入 ChromaDB 向量存储与 BM25 索引，供 HybridRetriever 查询。

# In Scope

- `scripts/ingest_kb.py` 入口脚本
- Markdown 文档解析与分块（按 frontmatter schema）
- BAAI/bge-large-zh-v1.5 嵌入生成
- ChromaDB 写入
- BM25 索引构建与持久化

# Out of Scope

- 实时增量入库（当前为全量重建）
- Qdrant 生产环境写入（Phase-005 范围）
- 文档质量校验 CI（见 REQ-012）

# Acceptance Criteria

- `python scripts/ingest_kb.py` 执行无报错
- ChromaDB 集合中可查到新入库文档
- BM25 索引中包含新入库文档
- 重复执行不产生重复文档

# Test Case Design Notes

> 需覆盖：首次入库、增量追加、重复执行幂等性。

| Acceptance Criteria | 覆盖状态 | 测试函数 |
|---|---|---|
| python scripts/ingest_kb.py 执行无报错 | ❌ TODO | 集成测试需 embedding 模型（跳过） |
| ChromaDB 集合中可查到新入库文档 | ✅ | `test_ingest_creates_chroma_collection` |
| BM25 索引中包含新入库文档 | ✅ | `test_ingest_creates_chroma_collection`（BM25 保存验证） |
| 重复执行不产生重复文档 | ❌ TODO | `test_ingest_is_idempotent`（`xfail`）— `add_documents()` 当前为追加语义，重复运行会使 collection count 翻倍；需为入库脚本添加 upsert-by-doc_id 或 pre-reset 逻辑后方可摘除 xfail 标记 |

**Note:** `test_ingest_creates_chroma_collection` 通过真实 `build_vectorstore()` / `add_documents()` / `BM25Index.load()` 链路（仅替换 embedding 为 16 维桩），覆盖 collection 命名、persist_dir、BM25 序列化。步骤 3 用真实持久化 Chroma collection（非 mock_vs）构建 `HybridRetriever` 并执行 `aretrieve()`，证明 ChromaDB 与 BM25 索引可一起被 HybridRetriever 查询。`test_ingest_is_idempotent` 标为 `xfail(strict=True)`，作为已知缺口的安全守门：实现修复后断言自动变 XPASS 提醒移除 xfail 标记。

# Agent Notes

已在 PHASE-001 中实现。入口在 `scripts/ingest_kb.py`，由 `bash scripts/local/ingest.sh` 调用。

code_refs:
  - scripts/ingest_kb.py   # KB ingestion entry point
